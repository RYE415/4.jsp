<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<script type="text/javascript" src="http://code.jquery.com/jquery-3.3.1.min.js">
</script>
<title>10.$.get().html</title>
<!-- 
[
  {
   "id":"1",
   "name":"레몬",
   "price":"3000",
   "description":"피로회복에 좋고 비타민C 풍부"
  },
  ,,,
 -->
<script>
  $(function(){
	  /*
	  	//json or xml 파일 불러오기->get 방식으로 $.ajax()를 요청해주는 함수
	  */
		//(2)$.getJSON('경로포함해서 불러올 파일명 or 사이트 주소',
		//				불러와서 처리할 function(매개변수명){})
		$.getJSON('item2.json',function(data){
			
		
			  //분리작업:파싱
			  $('#treeData').append('<tr><td>id</td>'+'<td>name</td>'+'<td>price</td><td>description</td></tr>');
	  	  /*
	  	  불러올 데이터가 태그 덩어리->$(반복선택자).each(~)=>$(this)
	  	  불러올 데이터가 태그 X -> $.each(컬렉션객체(배열),콜백함수->this)
	  	  */
	  	  $.each(data,function(){
	  		  $('#treeData').append('<tr><td>'+this.id+'</td><td>'
	  				  				+this.name+'</td><td>'
	  				  				+this.price+'</td><td>'
	  				  				+this.description+'</td></tr>');
	  	  })//each

	  })//$.getJSON
  })
</script>
</head>
<body>
	<table border="1" id="treeData"></table>
	
</body>
</html>